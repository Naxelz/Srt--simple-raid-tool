<Window x:Class="SimpleRaidTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleRaidTool"
        mc:Ignorable="d"
        Title="Simple Raid Tool" Height="600" Width="850"
        WindowStyle="None" ResizeMode="CanMinimize"
        Background="#050505"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#000000"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#222222"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#444444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C42B1C"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="25"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- TabControl Style -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- TabItem Style -->
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" BorderBrush="#333333" BorderThickness="1,1,1,0" CornerRadius="5,5,0,0" Margin="2,0">
                            <ContentPresenter x:Name="ContentSite"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            ContentSource="Header"
                                            Margin="10,5"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#222222" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#050505" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#333333" BorderThickness="1" Background="#050505" CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="$" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="15,0,0,0" HorizontalAlignment="Left"/>
                <TextBlock Text="Simple Raid Tool" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,5,0">
                    <Button Content="-" Style="{StaticResource TitleBarButton}" Click="MinimizeButton_Click"/>
                    <Button Content="✕" Style="{StaticResource CloseButton}" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>

            <!-- Inputs -->
            <StackPanel Grid.Row="1" Margin="20,10,20,10">
                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="- Token del bot:"/>
                    <TextBox x:Name="TokenBox" Grid.Column="1"/>
                </Grid>
                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="- ID del servidor:"/>
                    <TextBox x:Name="GuildIdBox" Grid.Column="1"/>
                    
                    <CheckBox Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                        <CheckBox.Template>
                            <ControlTemplate TargetType="CheckBox">
                                <Grid>
                                    <Border x:Name="SwitchBorder" Background="#333333" CornerRadius="10" Width="40" Height="20">
                                        <Ellipse x:Name="SwitchKnob" Fill="White" Width="16" Height="16" HorizontalAlignment="Left" Margin="2,0"/>
                                    </Border>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="SwitchBorder" Property="Background" Value="#b700fa"/>
                                        <Setter TargetName="SwitchKnob" Property="HorizontalAlignment" Value="Right"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </CheckBox.Template>
                    </CheckBox>
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="- Nombre Canal:"/>
                    <TextBox x:Name="ChannelNameBox" Grid.Column="1" Text="raid-channel"/>
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="- Nombre Rol:"/>
                    <TextBox x:Name="RoleNameBox" Grid.Column="1" Text="raided"/>
                </Grid>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="- Mensaje Spam:"/>
                    <TextBox x:Name="SpamMsgBox" Grid.Column="1" Text="Raid by Simple Raid Tool"/>
                </Grid>

            </StackPanel>

            <!-- Tabs -->
            <TabControl Grid.Row="2" Margin="20,10" Background="Transparent" BorderThickness="0">
                
                <TabItem Header="Destrucción">
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <Button Content="Raid Normal" Click="RaidButton_Click" Tag="raid_normal"/>
                            <Button Content="Raid Clásico" Click="RaidButton_Click" Tag="classic"/>
                            <Button Content="Raid RTool" Click="RaidButton_Click" Tag="raid_rtool"/>
                            <Button Content="Bypass Raid" Click="RaidButton_Click" Tag="bypass"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1">
                            <Button Content="Nuke (Eliminar Todo)" Click="RaidButton_Click" Tag="nuke"/>
                            <Button Content="ON (Full Destroy)" Click="RaidButton_Click" Tag="on"/>
                            <Button Content="Spam Mensajes" Click="RaidButton_Click" Tag="spam"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                             <Button Content="Abandonar Servidor" Click="RaidButton_Click" Tag="leave"/>
                             <Button Content="Cambiar Info Server" Click="RaidButton_Click" Tag="guild"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <TabItem Header="Creación / Spam">
                    <Grid Margin="0,10,0,0">
                         <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <Button Content="Crear Canales" Click="RaidButton_Click" Tag="crear_canales"/>
                            <Button Content="Crear Roles" Click="RaidButton_Click" Tag="crear_roles"/>
                            <Button Content="Crear Emojis" Click="RaidButton_Click" Tag="createemojis"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <Button Content="Crear Webhooks" Click="RaidButton_Click" Tag="wbspam"/>
                            <Button Content="Spamear Webhooks" Click="RaidButton_Click" Tag="existentwbspam"/>
                            <Button Content="Crear Mensajes" Click="RaidButton_Click" Tag="crear_mensajes"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                             <Button Content="Renombrar Roles" Click="RaidButton_Click" Tag="renameroles"/>
                             <Button Content="Renombrar Usuarios" Click="RaidButton_Click" Tag="massnick"/>
                             <Button Content="Dar Admin" Click="RaidButton_Click" Tag="guild.admin"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <TabItem Header="Moderación">
                     <Grid Margin="0,10,0,0">
                         <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <Button Content="Banear Todos" Click="RaidButton_Click" Tag="massban"/>
                            <Button Content="Banear Boosters" Click="RaidButton_Click" Tag="banboosters"/>
                            <Button Content="Banear Bots" Click="RaidButton_Click" Tag="banbots"/>
                            <Button Content="Banear Online" Click="RaidButton_Click" Tag="onlinemassban"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <Button Content="Desbanear Todos" Click="RaidButton_Click" Tag="massunban"/>
                            <Button Content="Kickear Todos" Click="RaidButton_Click" Tag="kickear_todos"/>
                            <Button Content="Silenciar Todos" Click="RaidButton_Click" Tag="silenciar_todos"/>
                        </StackPanel>
                        
                         <StackPanel Grid.Column="2">
                        </StackPanel>
                    </Grid>
                </TabItem>

                <TabItem Header="Limpieza">
                    <Grid Margin="0,10,0,0">
                         <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <Button Content="Eliminar Canales" Click="RaidButton_Click" Tag="eliminar_canales"/>
                            <Button Content="Eliminar Roles" Click="RaidButton_Click" Tag="eliminar_roles"/>
                            <Button Content="Eliminar Emojis" Click="RaidButton_Click" Tag="eliminar_emojis"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <Button Content="Eliminar Stickers" Click="RaidButton_Click" Tag="eliminar_stickers"/>
                            <Button Content="Eliminar Invitaciones" Click="RaidButton_Click" Tag="eliminar_invitaciones"/>
                            <Button Content="Eliminar Webhooks" Click="RaidButton_Click" Tag="eliminar_webhooks"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <TabItem Header="Token / Info">
                    <Grid Margin="0,10,0,0">
                         <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <Button Content="Token Info" Click="RaidButton_Click" Tag="token.info"/>
                            <Button Content="Token Guilds" Click="RaidButton_Click" Tag="token.guilds"/>
                            <Button Content="Token Admins" Click="RaidButton_Click" Tag="token.admin-guilds"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <Button Content="Token Owners" Click="RaidButton_Click" Tag="token.owner-guilds"/>
                            <Button Content="Token Leave All" Click="RaidButton_Click" Tag="token.leave-guilds"/>
                            <Button Content="Token Close DMs" Click="RaidButton_Click" Tag="token.close-mds"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                             <Button Content="Token Block Friends" Click="RaidButton_Click" Tag="token.block-friends"/>
                             <Button Content="Token Del Friends" Click="RaidButton_Click" Tag="token.delete-friends"/>
                             <Button Content="Token Theme Spam" Click="RaidButton_Click" Tag="token.theme-spam"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3">
                             <Button Content="Info Server" Click="RaidButton_Click" Tag="guild.info"/>
                             <Button Content="Ver Roles" Click="RaidButton_Click" Tag="guild.view-roles"/>
                             <Button Content="Ver Canales" Click="RaidButton_Click" Tag="guild.view-channels"/>
                             <Button Content="Ver Invitaciones" Click="RaidButton_Click" Tag="guild.invite"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

            </TabControl>
        </Grid>
    </Border>
</Window>
